export class UrlConfig {
    urlHost = "http://localhost:9000"
    urlLogin = this.urlHost + "/api/auth/login"
    urlAccount = this.urlHost + "/api/users"
    urlPosition = this.urlHost + "/api/job-position"
}